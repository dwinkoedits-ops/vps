name: VPS-Immortel-Final

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # 1. Connexion Tailscale (Nom fixe : chrish4x999hp)
      - name: Connexion Tailscale
        uses: tailscale/github-action@v2
        with:
          authkey: ${{ secrets.TAILSCALE_AUTHKEY }}
          hostname: chrish4x999hp
          ephemeral: true

      # 2. Montage du Disque Immortel (Décodage Base64)
      - name: Montage du Disque Immortel
        run: |
          sudo apt-get update && sudo apt-get install -y rclone
          mkdir -p ~/.config/rclone
          # Décodage propre du secret Base64
          echo "${{ secrets.RCLONE_CONFIG_TOKEN }}" | base64 -d > ~/.config/rclone/token.json
          # Création du fichier de config
          printf "[gdrive]\ntype = drive\nscope = drive\ntoken = %s\nteam_drive =\n" "$(cat ~/.config/rclone/token.json)" > ~/.config/rclone/rclone.conf
          mkdir -p ~/mon_disque
          rclone mount gdrive: /home/runner/mon_disque --daemon --allow-other
          
      # 3. Setup SSH avec MOT DE PASSE (vps123)
      - name: Setup SSH
        run: |
          sudo apt-get install -y openssh-server
          echo "runner:vps123" | sudo chpasswd
          sudo sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo service ssh restart

      # 4. Maintien en vie
      - name: Maintien du service
        run: |
          echo "--- VPS chrish4x999hp EN LIGNE ---"
          echo "Connecte-toi : ssh runner@chrish4x999hp"
          echo "Mot de passe : vps123"
          echo "Disque monté dans : ~/mon_disque"
          sleep 21600
